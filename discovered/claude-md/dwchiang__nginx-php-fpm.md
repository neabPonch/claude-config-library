---
name: dwchiang__nginx-php-fpm
source: https://github.com/dwchiang/nginx-php-fpm/blob/a123e5e8cc565f00578235458c11e2f0ac028012/CLAUDE.md
repo: dwchiang/nginx-php-fpm
kind: claude-md
stars: 179
last_pushed: 2026-05-17T08:33:10Z
license: apache-2.0
score: 9
domains: [devops, docker, backend]
tags: [docker-base-image, php-fpm, nginx]
curated: 2026-06-14
curated_by: config-scout
---

# dwchiang/nginx-php-fpm — claude-md

**Why it's worth keeping:** The 'do not hand-edit' warning prevents the AI from making futile changes to generated files, while providing exact environment variable sets for builds.

**Summary:** Defines a Docker base image project with critical guidance on build environments and runtime architecture.

**Source credibility:** Highly credible; well-starred repository with very recent maintenance.

**Recency:** Current; perfectly structured for modern agentic workflows.

**Source:** [dwchiang/nginx-php-fpm/CLAUDE.md](https://github.com/dwchiang/nginx-php-fpm/blob/a123e5e8cc565f00578235458c11e2f0ac028012/CLAUDE.md) · 179★

<details><summary>Excerpt (first 1200 chars)</summary>

```markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

A Docker base image (`dwchiang/nginx-php-fpm`) that merges nginx and php-fpm into a single container, managed by supervisord, for running Laravel and other PHP apps on cloud-native platforms (AWS Fargate, ECS/EKS, amd64/arm64). Images are published to Docker Hub and AWS ECR Public (`public.ecr.aws/n0b9n5m7/nginx-php-fpm`).

Two-part design:
- Part 1 (this repo): the base image Dockerfiles under `Dockerfiles/{OS}/`.
- Part 2 (user side): the consumer's own Dockerfile that `FROM`s this image and installs the PHP extensions their app needs. Reference Part 2 examples live in `Testings/` and are never published.

## Generated Dockerfiles — do not hand-edit

Every `Dockerfiles/{OS}/Dockerfile-*` file is generated by an external upstream toolkit (see the header comment in each file). They are mechanical merges of the official `php:*-fpm-*` and `nginx` Dockerfiles plus this project's supervisord/nginx config. Editing one file by hand is almost always wrong: the change will be lost on the next toolkit regeneration and will not propagate to the other ~hun
```

</details>
